cutback v0.1.0 - System for smaller, more focused backups

Usage: cutback [OPTIONS] [ACTION] [TYPE]

Options:
  -h, --help               Display help
  -v, --version            Display version
  -c, --config             Configuration file path
  -o, --output   PATH      Output path
  -d, --date     DATE      Date for the backup in YYYY-MM-DD format
  -i, --index    INDEX     Index for the backup
  -p, --paths    DIRS      Search paths
  -e, --excludes PATTERNS  Exclusion path patterns
  -r, --records  PATTERNS  Record path patterns
  -f, --format   FORMAT    Format of the metadata file
  -f, --format   FORMAT    Format of the metadata file
  -C, --compress           Enable archive compression
  -P, --progress           Enable viewing progress

Actions:
  generate                 Generate a file of the given type, or all types if none is given
  inspect                  Inspect a file of the given type, or all types if none is given

Types:
  manifest                 List of all files in the archive
  records                  List of files excluded from the archive, but kept a record of
  archive                  Archive of files in the manifest
  metadata                 Information about the backup

See `man 1 cutback` for more detailed information.
See `man 5 cutback` for configuration file information.
