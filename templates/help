<%= name %> v<%= version %> - <%= summary %>

Usage: <%= name %> [OPTIONS] [ACTION] [TYPE]

Options:
  -h, --help               <%= options["help"] %>
  -v, --version            <%= options["version"] %>
  -c, --config             <%= options["config"] %>
  -o, --output   PATH      <%= options["output"] %>
  -d, --date     DATE      <%= options["date"] %>
  -i, --index    INDEX     <%= options["index"] %>
  -p, --paths    DIRS      <%= options["paths"] %>
  -e, --excludes PATTERNS  <%= options["excludes"] %>
  -r, --records  PATTERNS  <%= options["records"] %>
  -f, --format   FORMAT    <%= options["format"] %>
  -P, --progress           <%= options["progress"] %>

Actions:
  generate                 <%= actions["generate"] %>
  inspect                  <%= actions["inspect"] %>

Types:
  manifest                 <%= types["manifest"] %>
  records                  <%= types["records"] %>
  archive                  <%= types["archive"] %>
  metadata                 <%= types["metadata"] %>

See `man 1 cutback` for more detailed information.
See `man 5 cutback` for configuration file information.
